import { base } from "$app/paths";

export const pathRoot = `${base}/`;

export const pathPlayground = `${base}/`;
export const pathChallenge = `${base}/challenge`;
export const pathManual = `${base}/manual`;
export const pathAbout = `${base}/about`;